/**
 * start-init.js — EventFlow /start page initialisation
 *
 * The template selector is now embedded in the wizard welcome screen
 * (rendered by start-wizard.js). This file is kept for any future
 * non-wizard startup tasks and backward compatibility.
 */
// No-op: template selection is now handled inside start-wizard.js
