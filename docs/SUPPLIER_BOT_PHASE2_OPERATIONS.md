# Phase 2 operations boundary

Source-code completion does not grant access to Railway. Production operators must configure the HMAC secret and ingestion enablement in Railway before live ingestion can be exercised. Phase 3 deliberately keeps publishing disabled, so those variables can be installed safely before any publication is authorised.
