name: Temporary telemetry formatter

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  format:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.node-version'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --prefer-offline --no-audit

      - name: Format telemetry bridge
        run: npx prettier --write services/backgroundJobTelemetryBridge.js

      - name: Upload formatted source
        uses: actions/upload-artifact@v4
        with:
          name: formatted-telemetry-bridge
          path: services/backgroundJobTelemetryBridge.js
          if-no-files-found: error
          retention-days: 1
